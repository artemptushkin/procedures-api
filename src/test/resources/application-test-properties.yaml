procedures:
  properties:
    createCats:
      name: create-cats
      type: MySQL
      parameters:
        name:
          procedureKey: name
          type: VARCHAR
    createDogs:
      name: create-dogs
      type: PostgreSQL
      parameters:
        name:
          procedureKey: name
          type: VARCHAR